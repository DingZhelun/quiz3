<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quiz3.quiz3.dao.Quiz3Dao">
    <select id="countPart1" resultType="java.lang.Integer">
        SELECT COUNT(0) FROM earthquake
        WHERE WHERE #{small_time2}&lt;=time2 AND time2&lt;=#{large_time2} AND magType=#{mag_type}
    </select>


</mapper>